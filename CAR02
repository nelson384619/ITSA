#include<stdio.h>
int main(){
    int a[7];
    int i;
    for(i=0; i<=5; i++){
        scanf("%d",&a[i]);
    }
    for(i=5; i>=1; i--){
        printf("%d ",a[i]);
    }
    printf("%d\n",a[i]);
    return 0 ;
}
