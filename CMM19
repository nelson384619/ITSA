#include<stdio.h>
#include<math.h>

int main(){
    int m;
    double cost;
    scanf("%d",&m);
    if(m<=800) cost=m*0.9;
    if(m>800 && m<1500) cost=m*0.81;
    if(m>=1500) cost=m*0.9*0.79;
    printf("%.1f\n",cost);
    return 0;
}
